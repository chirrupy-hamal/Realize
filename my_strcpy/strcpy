char *my_strcpy(char *dest, const char *src)
{
  assert(dest != NULL);
  assert(src != NULL);

  char *ret = dest;
  while(*dest = *src)
  {
    dest++;
    src++;
  }
  /*或者
  while(*dest++ = *src++);
  */
  return ret;
}
